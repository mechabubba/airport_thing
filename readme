install prerequisite requirements; `pip install -r requirements.txt`

create `config.ini` file;
```ini
[db]
host = "localhost"
user = "root"
password = "password"
database = "Team10_Deliverable4"
```
...fill in with your credentials

then run `python main.py`. :)


